---

- scm:
    name: git-ui-tests
    scm:
    - git:
        url: git@github.com:exarcist12/selenium_otus_students.git
        credentials-id: jenkins
        branches:
        - FETCH_HEAD
        refspec: ${BRANCH}

- scm:
    name: git-runner
    scm:
    - git:
        url: git@github.com:exarcist12/jenkins2.git
        credentials-id: jenkins
        branches:
        - FETCH_HEAD
        refspec: ${BRANCH}